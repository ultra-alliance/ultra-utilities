// istanbul ignore file

/**
 * `tLanguage` is a type representing a language. It includes a `code` and `label` property.
 *
 * @category Models
 */

type tLanguage = {
  code: string;
  label: string;
};

export default tLanguage;
